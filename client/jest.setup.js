// jest.setup.js
import "@testing-library/jest-dom";

// Optional: Configure jest to handle CSS imports
// If you're using CSS modules, you might need to mock them
// jest.mock('./path/to/your/css/module.css', () => ({}))
